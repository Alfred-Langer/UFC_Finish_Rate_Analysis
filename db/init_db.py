# from db.session import engine
# from models.base import Base

# # Initialize database (create tables)
# def init_db():
#     from models import bout, event, fighter  # Import all models
#     Base.metadata.create_all(bind=engine)